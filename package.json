{
  "name": "taskflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && rm -rf public && mkdir -p public && cp -r client/dist/* public/",
    "install-all": "npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
