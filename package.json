{
  "name": "taskflow",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && node scripts/postbuild.js",
    "install-all": "npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
